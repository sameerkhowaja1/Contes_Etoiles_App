class StoryModel {
  String storyName;
  String storyImage;
  DateTime totalTime;
  DateTime timeRemaining;

  StoryModel({required this.storyName, required this.storyImage, required this.totalTime, required this.timeRemaining});
}
